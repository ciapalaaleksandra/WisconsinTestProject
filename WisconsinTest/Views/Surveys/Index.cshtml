@model IEnumerable<WisconsinTest.Models.Surveys>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.Date)*@
                @Html.ActionLink("Nazwisko", "Index", new { sortOrder = ViewBag.NameSortParam})
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Patients.Name)*@
                @Html.ActionLink("Data", "Index", new { sortOrder = ViewBag.DateSortParam})
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Patients.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.SurveyId }) |
                @Html.ActionLink("Details", "Details", new { id=item.SurveyId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.SurveyId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
